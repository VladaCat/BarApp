import 'package:flutter/material.dart';

class UserRandom extends StatelessWidget {
  // const UserRandom({Key?key}):super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Random',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
